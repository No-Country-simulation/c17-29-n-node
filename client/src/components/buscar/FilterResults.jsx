import React from 'react'

export const FilterResults = (travels) => {
    return (
        <article className='flex flex-col gap-4'>
            <article className='flex flex-col gap-4'>
                {/* {travels.map(travel => (
                    <FilterCard travel={travel} key={travel._id} />
                ))} */}
            </article>
        </article>
    )
}
